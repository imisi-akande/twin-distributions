from setuptools import setup

setup(name='twin_distribution',
      version='0.1',
      description='Normal and Binomial distributions',
      packages=['twin_distribution'],
      install_requires=['matplotlib'],
      zip_safe=False)